import { createClient } from 'https://esm.sh/@supabase/supabase-js@2';
const cors={'Access-Control-Allow-Origin':'*','Access-Control-Allow-Headers':'authorization, x-client-info, apikey, content-type'};
Deno.serve(async(req)=>{if(req.method==='OPTIONS')return new Response('ok',{headers:cors});try{const {order_id}=await req.json();const sb=createClient(Deno.env.get('SUPABASE_URL')!,Deno.env.get('SUPABASE_SERVICE_ROLE_KEY')!);const {data:o,error}=await sb.from('custom_orders').select('*').eq('id',order_id).single();if(error)throw error;const text=`🎨 CacaoForm — нова custom заявка\n\n${o.customer_name}\n${o.email}\n${o.phone}\n\n${o.description}${o.file_url?`\n\nФайл: ${o.file_url}`:''}`;const bot=Deno.env.get('TELEGRAM_BOT_TOKEN'),chat=Deno.env.get('TELEGRAM_CHAT_ID');if(bot&&chat)await fetch(`https://api.telegram.org/bot${bot}/sendMessage`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({chat_id:chat,text})});return new Response(JSON.stringify({ok:true}),{headers:{...cors,'content-type':'application/json'}})}catch(e){return new Response(JSON.stringify({error:String(e)}),{status:500,headers:{...cors,'content-type':'application/json'}})}});
